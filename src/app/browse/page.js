import BrowseClient from "@/components/browse/BrowseClient";

export const metadata = {
  title: "Browse Listings | TrustKar",
  description: "Search and filter escrow-protected listings across Pakistan.",
};

export default function BrowsePage() {
  return <BrowseClient />;
}
